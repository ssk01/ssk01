# Socrates — 问答知识库

### Q: v1（缓存分块）为什么反而干不过 plain ikj？
在 N≤2048 这个规模，v1 的分块是「负优化」，三个原因：
1. **要解决的问题不存在**：B（4–16MB）本就装得进 16MB 共享 L2，plain ikj 扫 B 是从 L2 流出、非 DRAM，分块省 DRAM 流量的收益≈0。
2. **内层循环被切短**：ikj 内层 `for j` 跑满整行 N（长连续向量流），v1 只在 jj 块内跑 64 步 → 循环控制/尾数/预取热身占比上升，稳态吞吐掉。
3. **对 K 分块给 C 加了访存流量**：v1 嵌套 `ii→kk→jj→i→k→j`，同一 C 块在每个 kk 都被读-改-写一遍；两次访问间扫过整条 ii 行带(≈256KB>128KB L1)把它挤出 L1，每个 kk 都重搬 C。而 ikj 对固定 i 一次扫完所有 k，C 行常驻 L1、只过一遍。
根因：两者都无寄存器复用（AI≈0.5，load-bound），v1 唯一优势(B 复用)因 B 从 L2 本就便宜而无用，额外开销却是实打实的。教训：分块必须配寄存器微内核（=v2）才有意义。
(2026-07-06 16:45)

### Q: cblas_sgemm 是怎么实现的？为什么快到 ~3200 GFLOPS？
Apple Accelerate 的 cblas_sgemm 内部是高度调优的分块 GEMM（思路和 v2/v3 一致：打包+微内核+多级分块+多线程），但微内核跑在 **AMX（Apple Matrix Extension）矩阵协处理器**上，而非 NEON。AMX 是 CPU 簇共享的专用矩阵引擎，做**外积累加**——一条指令把行向量×列向量的外积加到整块寄存器 tile，单周期 FMA 数远超 128-bit NEON。实测 ~3200 GFLOPS ≈ openmp 12× ≈ v3 3.6×。结论：NEON 手写到 v3(~850) 已吃满通用 SIMD 流水线，真正天花板由专用矩阵硬件(AMX)决定，纯 intrinsics 够不到；生产环境 GEMM 就该用 BLAS。
(2026-07-06 16:50)

### Q: 为什么 v3 能赢 OpenMP 3~4 倍（而不是靠线程更多）？
两者线程数一样、并行加速也都只有 ~8×（本机 10P+4E、共享带宽）。胜负在单核：openmp/ikj 每载入 1 个 B 元素只用 1 次(AI≈0.5)，卡在 L1/L2 load 带宽；v2 的 8×8 微内核每步载 64B 做 128 flops(AI≈2)，64 个累加器全程驻寄存器、A 复用 8 列/B 复用 8 行 → 翻转成算力受限。单核 v2(110) vs ikj(32)≈3.4× 的代差，原样传递为 v3 vs openmp 的 ~3.1×。
(2026-07-06 16:50)

<!-- 以下继续记录 -->
